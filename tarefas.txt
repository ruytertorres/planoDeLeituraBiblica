📘 NOTA DE CONTINUIDADE — APP DE LEITURA BÍBLICA

Data: 17/01/2026
Estado: Arquitetura saudável, evolução controlada, sem dívida técnica

1. O QUE FOI FEITO — BLOCO DE NOTAS (UI BASE)
✅ Bloco de Notas Flutuante — Integração Inicial

Status: FUNCIONAL (fase UI base)

Implementado com sucesso:

Botão flutuante fixo (#btn-notas)

Overlay flutuante (#notas-overlay)

Editor via contenteditable

Persistência automática em localStorage

Limpeza com confirmação

Integração correta com index.html e main.js

Decisão arquitetural importante:

O HTML da nota é estático

O JS apenas controla comportamento

Nenhum elemento de UI é criado dinamicamente

Isso garante:

Previsibilidade visual

Manutenção simples

Compatibilidade futura com Tailwind e PWA

✅ Separação de responsabilidades (Notas)

Estrutura validada:

/css
  └── notas.css
      → Estilo exclusivo do bloco de notas

/js/dominio/notas
  └── NotasLeituraManager.js
      → Regra de negócio + persistência

/js/ui/notas
  ├── notas_overlay.js
  └── notas_toolbar.js (criado, ainda vazio)


NotasLeituraManager:

Armazena HTML bruto

Não conhece DOM

Usa localStorage

Já preparado para múltiplos usuários

2. continuação de manutenção no bloco de notas flutuante

estado atual 17/01/26

Estado geral

✔ Arquitetura saudável
✔ Separação domínio / UI bem encaminhada
✔ Bloco de notas corretamente incompleto
✔ Nenhum erro grave de concepção
Estrutura do bloco estática no index.html
✔ JS apenas controla comportamento
✔ Nenhum elemento estrutural criado via JS

Isso está 100% alinhado com:

PWA

Tailwind

Manutenção previsível

estamos nesse ponto: 


Opção A — Arquitetural

Definir com você o contrato HTML semântico final

Sem código ainda

Opção B — Técnica

Implementar notas_toolbar.js forçando esse contrato

IMPLEMENTAÇÃO DE notas_toolbar.js (FORÇANDO O CONTRATO)

Agora sim, código — somente UI, sem domínio.

Responsabilidade

Executar comandos restritos

Nunca permitir HTML fora do contrato

Não persistir nada

Sem permitir HTML inválido

/js/ui/notas/notas_toolbar.js

Opção C — Segurança

Criar função de normalização que limpa HTML inválido automaticamente

NORMALIZAÇÃO E SEGURANÇA

/js/dominio/notas/normalizar_html_nota.js

>> teste de funcionalidade falhou:

Quando esse erro sumir:

Teste colar HTML externo no editor (ex: <div>, <img>) [o bloco de notas está aceitando html invalido]

Ele deve desaparecer automaticamente

Apenas tags do contrato devem sobreviver

Quando confirmar isso, o sistema está oficialmente blindado.

Se aparecer qualquer outro erro, mande exatamente como você fez agora — com mensagem completa.


alteração sugerida (porem falhou)

4. AJUSTE CANÔNICO EM notas_overlay.js

Substitua apenas o listener de input por este:

editor.addEventListener("input", () => {
  notasManager.setConteudo(editor.innerHTML);

  const htmlNormalizado = notasManager.getConteudo();

  if (editor.innerHTML !== htmlNormalizado) {
    const pos = window.getSelection()?.getRangeAt(0);
    editor.innerHTML = htmlNormalizado;
  }
});

O que isso garante:

✔ HTML inválido desaparece imediatamente (falhou)
✔ Colagens externas são limpas na hora(falhou)
✔ O editor nunca exibe algo que o domínio não aceita(falhou)


4 O QUE NÃO FOI FEITO (INTENCIONALMENTE)

Nada abaixo é bug ou atraso — são etapas conscientemente não iniciadas:

Toolbar funcional (bold, itálico, título) [estamos nessa fase]

Normalização semântica do HTML das notas [estamos nessa fase]

Conversão HTML → DOCX [proxima etapa apos corrigir o erro]

Associação da nota ao dia

Associação da nota ao usuário

Progresso desacoplado da UI (Service)

Esses passos não foram antecipados para evitar acoplamento e retrabalho.


🔹 PASSO 6 — BLOCO DE NOTAS (CONTINUAÇÃO)

Ordem correta (já definida):

Toolbar real (bold / itálico / título)
Arquivo: /js/ui/notas/notas_toolbar.js

Normalização de HTML
Converter HTML livre em HTML semântico previsível

Conversão HTML → DOCX
Exportação client-side + opção de limpar

Associação da nota ao dia / usuário
Estrutura alvo:

notas[usuarioId][planoId][diaNumero] = html

DECISÕES DE ARQUITETURA REGISTRADAS

O Bloco de Notas pertence ao domínio do usuário, não à UI

Hoje usa localStorage

Amanhã suportará múltiplos usuários sem reescrita

Nenhuma decisão tomada hoje gera dívida técnica

🔹 PASSO 7 — cetificado e limpesa de progresoo

- adcionar um botão que limpa o progrsso de leitura voltando ele a 0 dias lidos; essa ação precisa de confirmação, um sistema de segurança que 
impede de limpar acidentalmente
-durante a leitura anual a qualquer momento fica um botão de expotação de progreço que envaia um docx com o progresso para o usuario sontendo:
progresso em %
total de dias lidos
total de capitulos lidos
qual o ultimo dia e o ultimo capitulo lido, qual o proximo dia e proximo capitulo
- no fim da leitura emite um certificado simbolico contendo:

leitura anual da biblia
conclida por usuario; no periodo do dia xx/xx/xxxx até xx/xx/xxxx
"texto generico de certificado"

imlementações futuras

1. botão de redefinir progresso do plano de leitura (zerando ele com apenas um click); esse botã oprecisa de um seistema 
de segurança de validação para não resetar o progrço por acidentalmente
2. certificado de conclusão de leitura da biblia (gerar um certificado de conclusão de leitura da biblia com base no plano atual de escolha do usuario [com o nome do usuaro; data de inicio; data de termino; texto de certificado generico])
esse certificado não pode ser gerado com menos de 90 dias apos o inicio da leitura, para evitar fraude